[package]
name = "axum-memory-example"
version = "0.1.0"
edition = "2021"

[dependencies]
multigear = { path = "../..", features = ["axum"] }
axum = "0.7"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }


